{
    "name": "fastapi",
    "version": "0.1.0",
    "private": true,
    "scripts": {
      "fastapi-dev": "pip3 install -r requirements.txt && python3 -m uvicorn api.index:app --reload",
      "next-dev": "next dev",
      "dev": "uvicorn madsoft.main:app --reload --port 8001",
      "m": "alembic revision --autogenerate",
      "u": "alembic upgrade head",
      "d": "alembic downgrade -1"
    }

  }